import { Link } from "@tanstack/react-router"

export const Header  = () => {
    return( 
        <header className="bs-body-bg">
        <div className="container">
        </div>
        </header>
    )
}